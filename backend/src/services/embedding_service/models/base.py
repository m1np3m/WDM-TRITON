from abc import ABC

class EmbeddingService(ABC):
    """
    Abstract base class marker for embedding services.
    Does not define any methods, used for type hierarchy and enforcement.
    """
    pass
